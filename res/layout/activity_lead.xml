<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".LeadActivity" >

    <android.support.v4.view.ViewPager
        android:id="@+id/vp_lead"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_centerInParent="true" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="80dp"
        android:orientation="horizontal" >

        <ImageView
            android:id="@+id/iv_lead_left"
            android:layout_width="@dimen/lead_image_width"
            android:layout_height="@dimen/lead_image_height"
            android:contentDescription="one"
            android:src="@drawable/bg_myitem_islike_new" />

        <ImageView
            android:id="@+id/iv_lead_middle_left"
            android:layout_width="@dimen/lead_image_width"
            android:layout_height="@dimen/lead_image_height"
            android:layout_marginLeft="5dp"
            android:layout_marginRight="5dp"
            android:contentDescription="two"
            android:src="@drawable/bg_myitem_like_new" />

        <ImageView
            android:id="@+id/iv_lead_middle_right"
            android:layout_width="@dimen/lead_image_width"
            android:layout_height="@dimen/lead_image_height"
            android:layout_marginRight="5dp"
            android:contentDescription="three"
            android:src="@drawable/bg_myitem_like_new" />

        <ImageView
            android:id="@+id/iv_lead_right"
            android:layout_width="@dimen/lead_image_width"
            android:layout_height="@dimen/lead_image_height"
            android:contentDescription="four"
            android:src="@drawable/bg_myitem_like_new" />
    </LinearLayout>

    <TextView 
		android:id="@+id/txt_lead"
    	android:layout_width="wrap_content"
        android:layout_height="wrap_content" 
        android:text="跳过"
        android:textSize="15dp"
        android:textColor="@color/black"
        android:layout_alignParentRight="true"
        android:layout_alignParentBottom="true"
        android:layout_margin="35dp"
        android:visibility="invisible"
	    />
    
</RelativeLayout>